# Copy to .env for local development:  cp .env.example .env
NODE_ENV=development
PORT=4000

DATABASE_URL=postgres://historian:historian@localhost:5432/history_timeline

# Only needed if you serve the browser app from a DIFFERENT origin than the API.
# In development the API serves client/ itself, so this stays empty.
CORS_ORIGINS=
